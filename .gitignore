# Local runtime data & build output (never share)
data/
bin/
target/

# Logs, local test artifacts & scripts
*.log
*.txt
*.ps1

# Local environment overrides (never commit)
.env

# Allow the committed one-command runner (everything else *.ps1 stays ignored)
!start.ps1

package com.example.my_first_spring_api.model;

/**
 * Food categories used for buyer-side discovery. 'SPECIAL' backs festive /
 * limited-time dishes that are surfaced in the UI but not shown as a fixed tile.
 */
public enum Category {
    BREAKFAST,
    LUNCH,
    DINNER,
    SNACKS,
    SPECIAL
}
